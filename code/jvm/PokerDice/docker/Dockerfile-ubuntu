FROM ubuntu:22.04

# Install dig (from dnsutils package) and other useful tools
RUN apt-get update && \
    apt-get install -y dnsutils curl iputils-ping net-tools && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Keep container running
CMD ["tail", "-f", "/dev/null"]
